#include <bits/stdc++.h>

using namespace std;

#define REP(i, n) for (int i = 0; i < (int)(n); i++)
#define REP2(i, s, n) for (int i = (s); i < (int)(n); i++)
#define REQ(i, n) for (int i = 1; i <= (int)(n); i++)
#define REQ2(i, s, n) for (int i = (s); i <= (int)(n); i++)
#define ALL(x) (x).begin(), (x).end()

using ll = long long;

int n, a[100005];

int main() {
  cin >> n;

  REP(i, n) cin >> a[i];

  return 0;
}
